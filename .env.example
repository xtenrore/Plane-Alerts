# =============================================================================
# Plane Alerts v5.5.1 - aircraft spotting alerts and spotting intelligence
# =============================================================================
# Copy this file to .env for local development/self-hosting. Never commit secrets.

# --- Telegram ---
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
WEBHOOK_URL=
WEBHOOK_SECRET=

# --- Database ---
# Native guided self-hosting defaults to the local SQLite backend. Docker Compose
# overrides DATABASE_BACKEND and MONGO_URI to use its bundled MongoDB service.
DATABASE_BACKEND=sqlite
SQLITE_PATH=
MONGO_URI=mongodb://localhost:27017
DATABASE_NAME=aircraft_bot

# --- Prediction Lab evidence storage ---
# Native guided installs replace this with a persistent path beside their local
# community data. Docker Compose sets /data/prediction_lab on a named volume.
PREDICTION_LAB_ROOT=./.community/prediction_lab

# --- Public aircraft data providers ---
ADSB_LOL_BASE_URL=https://api.adsb.lol/v2
ADSB_FI_BASE_URL=https://opendata.adsb.fi/api/v2
AIRPLANES_LIVE_BASE_URL=https://api.airplanes.live/v2
ADSB_ONE_BASE_URL=https://api.adsb.one/v2
OPENSKY_BASE_URL=https://opensky-network.org/api
OPENSKY_TOKEN_URL=https://auth.opensky-network.org/auth/realms/opensky-network/protocol/openid-connect/token
OPENSKY_1=
OPENSKY_2=
OPENSKY_3=
OPENSKY_4=
OPENSKY_5=
OPENSKY_CREDENTIALS_JSON=
API_KEYS_DIR=api

# --- Optional local ADS-B receiver ---
LOCAL_ADSB_URL=
LOCAL_ADSB_RECEIVER_TYPE=auto
LOCAL_ADSB_TIMEOUT_SECONDS=0.8
LOCAL_ADSB_MAX_POSITION_AGE_SECONDS=12
LOCAL_ADSB_AUTH_HEADER=
# Receiver coordinates describe receiver coverage only; they are not Telegram
# observer/profile locations and are not written to Prediction Lab Git evidence.
LOCAL_ADSB_RECEIVER_LATITUDE=
LOCAL_ADSB_RECEIVER_LONGITUDE=
LOCAL_ADSB_RECEIVER_COVERAGE_KM=150

# --- Optional photography / explanation services ---
GEMINI_API_KEY=
GEMINI_API_KEY_2=
GEMINI_MODEL_PRIMARY=gemini-3.5-flash-lite
GEMINI_MODEL_SECONDARY=gemini-3.5-flash
GEMINI_PHOTO_MODEL=gemini-3.8-flash
GEMINI_PHOTO_FALLBACK_MODEL=gemini-3.5-flash-lite
GEMINI_PHOTO_TIMEOUT_SECONDS=30
GROQ_KEY=
GROQ_KEY_2=
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
GOOGLE_CONTRAILS_API_KEY=

# --- Open-Meteo photography conditions ---
OPEN_METEO_FORECAST_URL=https://api.open-meteo.com/v1/forecast
OPEN_METEO_AIR_QUALITY_URL=https://air-quality-api.open-meteo.com/v1/air-quality
PHOTOGRAPHY_HTTP_TIMEOUT_SECONDS=12
PHOTOGRAPHY_CONDITIONS_CACHE_SECONDS=120

# --- Monitoring ---
POLL_INTERVAL_SECONDS=5
DEFAULT_RADIUS_KM=15.0
COOLDOWN_MINUTES=30
LEARNING_PLANE_THRESHOLD=100
RELEARN_PLANE_COUNT=25

# --- v5.2+ shadow evaluation (evaluation-only; never selects the live model) ---
PLANE_SHADOW_EVALUATION_ENABLED=true
PLANE_SHADOW_V46_LINEAR_ENABLED=true
PLANE_SHADOW_V46_TURN_ENABLED=true

# --- Admin ---
# Blank ADMIN_TELEGRAM_ID is valid and means no Telegram root ID is configured.
# Admin HTTP routes fail closed while ADMIN_PASSWORD is blank. Set a strong
# password before using the web admin dashboard/API.
ADMIN_TELEGRAM_ID=
ADMIN_PASSWORD=

# --- Server / Compose host port ---
HOST=0.0.0.0
PORT=8000
PLANE_ALERTS_PORT=8000
LOG_LEVEL=INFO
